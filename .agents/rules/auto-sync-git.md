# Regla de Sincronización Automática con Git / Vercel

Siempre que se realicen y verifiquen cambios en el código del proyecto, hacer commit y push a la rama `main` en GitHub (`origin main`) para que Vercel despliegue la versión actualizada automáticamente.
